package com.chat.app.backend.feature.user.model;

/**
 * Enum representing the possible status values for a user in the chat application.
 */
public enum UserStatus {
    ONLINE,
    OFFLINE,
    AWAY,
    DO_NOT_DISTURB,
    INVISIBLE
}